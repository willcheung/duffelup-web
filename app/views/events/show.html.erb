<h2><%= @event.title %></h2>
<br/>
<div style="max-height:380px; overflow:auto;">
<% if @event.eventable_type == "Activity" or @event.eventable_type == "Hotel" or @event.eventable_type == "Foodanddrink" %>

	<% if @event.photo? %>
	<div class="event-photo-show">
		<img src="<%= get_image_url(@event, "large") %>" alt="<%= @event.title %>" />
	</div>
	<% end %>

	<div id="form-details" <% if @event.photo? %>style="width:340px"<% end %>>
	<%= (link_to h(@idea.website), @idea.website) unless @idea.website.blank? %><br/>
	<%= "<h3>Address</h3>" + content_tag('p', h(@idea.address)) + "(" + map_link("map", @idea.address) + ")" unless @idea.address.blank? %>
	<%= "<h3>Phone</h3>" + content_tag('p', h(@idea.phone))  unless @idea.phone.blank? %>
	
	<% if @event.note? %>
		<h3>Notes</h3>
		<div id="note"><%= simple_format_without_p(h(@event.note)) %></div>
	<% end %>
	</div>
	
<% elsif @event.eventable_type == "Transportation" %>

	<div id="form-details">
	<%= h @transportation.from %> &rarr; <%= h @transportation.to %><br/>
	Departs <%= h @transportation.departure_time.strftime("%a %m/%d at %I:%M%p") unless @transportation.departure_time.nil? %> <br/>
	Arrives <%= h @transportation.arrival_time.strftime("%a %m/%d at %I:%M%p") unless @transportation.arrival_time.nil? %><br/><br/>
	
	<% if @event.note? %>
		<h3>Notes</h3>
		<div id="note"><%= simple_format_without_p(h(@event.note)) %></div>
	<% end %>
	</div>

<% elsif @event.eventable_type == "Notes" %>
	<div id="form-details">
	<%= simple_format_without_p(h(@notes.content)) %>
	</div>
<% end %>

</div>